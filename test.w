;; example program for testing
;; this thing totally shouldn't be included in the git repo

; (defun fact (x)
;   (if (= x 0)
;       1
;       (* x (fact (- x 1)))))

; (print (fact 5))

(setq a 3)

(print (+ a a))
